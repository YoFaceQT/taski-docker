name: Main Taski Workflow
on:
  push:
    branches:
      - main
jobs:
  checkout-and-print-tree:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository codeм сами
        uses: actions/checkout@v3теки GitHub Actions
      - name: Print project tree
        run: tree .

